import styled from 'react-emotion';

const Page = styled('div')({
  width: '100%',
  height: '100%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'flex-start',
  paddingTop: 250,
  paddingBottom: 100,
  background: '#f7f7f7'
});

export default Page;
