import styled from 'react-emotion';

const Page = styled('div')({
  width: '100%',
  height: '100vh',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'flex-start',
  paddingTop: 250,
  background: '#f7f7f7'
});

export default Page;
